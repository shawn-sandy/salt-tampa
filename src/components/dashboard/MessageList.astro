---
import type { Message } from '#libs/database-types'

export type Props = {
  messages: Message[]
  classes?: string
}

const { messages, classes } = Astro.props
---

<ul class=`messages-list messages ${classes}` data-list="unstyled">
  <li>
    <p class="message-list">
      Found {messages.length} message{messages.length !== 1 ? 's' : ''}
    </p>
  </li>
  {
    messages.map(message => (
      <li data-message-id={message.id}>
        <h3>{message.subject}</h3>
        <p>
          <strong>From:</strong> {message.name}{' '}
          <a href={`mailto:${encodeURIComponent(message.email)}`}>{message.email}</a>
          <br />
          <strong>Date:</strong> {new Date(message.created_at).toLocaleString()}
        </p>
        <p>{message.message}</p>
        <hr />
      </li>
    ))
  }
</ul>

<style>
  .messages-list {
    max-width: 800px;
    margin: 0 auto;
    padding: 1rem;
    text-align: left;
  }

  .messages-list p {
    text-align: left;
  }

  .messages {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
</style>
